version: '3.8'
services:
  backend:
    build:
      context: ./backend
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/data
    env_file: .env
    restart: unless-stopped

volumes:
  sqlite_data:
